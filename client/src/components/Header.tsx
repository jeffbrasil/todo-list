import React from 'react';

const Header = () => {
  return (
    <header>
      <h2>ToDo List</h2>
      {/* Adicionar links de navegação se necessário */}
    </header>
  );
};

export default Header;
